name: hoffelijk_quiz_app
description: Simple quiz app for demo'ing my capabilities in flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.0
  equatable: ^2.0.3
  sizer: ^2.0.15


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
